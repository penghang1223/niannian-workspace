{
  "name": "api-schema-contract-system",
  "version": "1.0.0",
  "description": "API Schema Contract System to prevent frontend/backend field inconsistency",
  "main": "lib/api-schema-validator.ts",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "generate-types": "ts-node lib/api-schema-examples/generate-types.ts",
    "validate": "python3 lib/debug-validate.py"
  },
  "keywords": ["api", "schema", "contract", "validation", "typescript"],
  "author": "OPC Team",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ajv": "^8.0.0",
    "json-schema": "^0.4.0"
  }
}