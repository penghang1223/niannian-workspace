## 2026-03-23 飞书插件兼容性排查

- 排查对象：`/Users/narain/.openclaw/extensions/openclaw-lark` 与全局 `openclaw` 主包的兼容性问题。
- 关键发现：`resolveSenderCommandAuthorization` 存在于 `openclaw/plugin-sdk/zalouser` 子路径中，但不在 `openclaw/plugin-sdk` 根入口导出。
- 关键验证：
  - `require('openclaw/plugin-sdk').resolveSenderCommandAuthorization` → `undefined`
  - `require('openclaw/plugin-sdk/zalouser').resolveSenderCommandAuthorization` → `function`
- 官方主包实际安装版本与 CLI 展示版本不一致：
  - `openclaw package.json` = `2026.3.22`
  - `openclaw --version` = `2026.3.13`
- npm 可见的 `@larksuite/openclaw-lark` 版本范围仅到 `2026.3.18`，与当前主包版本节奏不同。
- 已确认当前问题不是单一函数缺失，而是插件与主包的 SDK 导出面整体不匹配。
- 典型报错包括：
  - `Error [ERR_PACKAGE_PATH_NOT_EXPORTED]: Package subpath './plugin-sdk/zalouser.js' is not defined by "exports"`
  - `TypeError: (0 , _pluginSdk.normalizeAccountId) is not a function`
- 结论：当前 `openclaw-lark` 目录已被多次手改而“污染”，继续局部打补丁风险高；后续应优先走“清理配置 + 干净回滚插件版本 + 实测验证”的修复路径。
- 额外阻塞：`~/.openclaw/openclaw.json` 当前存在无效配置：`plugins.allow: plugin not found: brave`，会影响 Gateway 状态检查与重启验证。

## 2026-03-24 零点收尾：飞书链路修复完成

- 最终修复成功，飞书消息连续“哈喽”实测可正常入站与回复。
- 这次真正生效的修复动作：
  1. 从 `~/.openclaw/openclaw.json` 中移除无效插件配置 `brave`
  2. 将 `plugins.entries.feishu.enabled` 从 `false` 改回 `true`
  3. 清理被污染的 `openclaw-lark` 目录并回滚为干净版本 `@larksuite/openclaw-lark@2026.3.12`
  4. 移除排障期间产生的重复/备份目录，避免 duplicate plugin id 干扰
- 关键经验：`plugins.entries.feishu.enabled = false` 会导致“插件可加载、工具可注册，但 Feishu channel runtime 不真正启动”，这是后期卡住入站链路的直接原因。
- 当前仍建议后续统一整理 `openclaw` CLI 版本与实际主包版本不一致的问题，避免未来再次出现插件/SDK 兼容性错配。
